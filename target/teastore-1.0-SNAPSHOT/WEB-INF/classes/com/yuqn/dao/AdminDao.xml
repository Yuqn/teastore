<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuqn.dao.AdminDao">
    <select id="selAdmin" resultType="com.yuqn.entity.Admin">
        select admin_id,admin_card,admin_password,admin_name from admin where admin_card=#{admin_card} and admin_password=#{admin_password}
    </select>
</mapper>